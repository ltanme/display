
# 工具相关配置
gfcli:
  # 工具编译配置
  build:
    name:     "character"
    arch:     "arm"
    system:   "linux,darwin,windows"
    mod:      ""
    cgo:      0

  # dao生成
  gen:
    dao:
      - link:            "mysql:root:12345678@tcp(127.0.0.1:3306)/focus"
        removePrefix:    "gf_"
        descriptionTag:  true
        noModelComment:  true
